name: remote_webrtc_client
publish_to: 'none'
description: RemoteWebRTC Flutter client MVP
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_webrtc: ^0.11.6

flutter:
  uses-material-design: true
